syntax = "proto3";

package connect;

option go_package = "https://github.com/JonasUJ/dsys-hw2;connect";

service Connect {
    rpc Connect (stream Message) returns (stream Message);
}

message Message {
    uint64 time = 1;
    uint32 pid = 2;
}
